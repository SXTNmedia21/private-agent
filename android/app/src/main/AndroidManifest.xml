<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions.
         P2 deleted the on-device assistant features (SMS, calls, contacts, mic, alarms,
         audio/system settings), so their permissions are gone with them: the phone is a dumb
         executor and must not hold authority it no longer uses. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- In-app self-update: install the downloaded APK -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Bridge connection stays alive across screen-off and reboot -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required from targetSdk 34; harmless on API 26..33 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- P6 recording. From Android 10 a MediaProjection capture requires a foreground service
         declaring the mediaProjection type; without it createVirtualDisplay throws a security
         exception that reads like a consent problem and sends you looking in the wrong place. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- The only sanctioned way to implement closeApp; normal permission, no runtime prompt.
         It reaps background processes only — see DeviceChannel.closeApp. -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:label="PrivateAgent"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Shizuku provider (optional - only works when Shizuku is installed) -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Accessibility Service for screen reading and UI automation -->
        <service
            android:name=".AgentAccessibilityService"
            android:exported="true"
            android:label="PrivateAgent Screen Control"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Notification read/act surface. Bound by the system only after the user grants
             notification access in Settings. -->
        <service
            android:name=".AgentNotificationListenerService"
            android:exported="true"
            android:label="PrivateAgent Notifications"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Keeps the bridge WebSocket alive; One UI kills background sockets aggressively. -->
        <service
            android:name=".AgentForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaProjection" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
